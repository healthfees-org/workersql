import { test, expect } from '@playwright/test';

test('home page renders', async ({ page }) => {
  // This is a placeholder; actual run requires deployed worker serving assets
  // Basic page check to avoid unused parameter warning
  expect(page).toBeDefined();
});
