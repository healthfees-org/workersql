package com.workersql.sdk.types;

/**
 * Error codes for WorkerSQL operations
 */
public enum ErrorCode {
    INVALID_QUERY,
    CONNECTION_ERROR,
    TIMEOUT_ERROR,
    AUTH_ERROR,
    PERMISSION_ERROR,
    RESOURCE_LIMIT,
    INTERNAL_ERROR
}
